name: nyc_taxi_lakehouse_dbt
version: "1.0.0"
config-version: 2

profile: nyc_taxi_lakehouse

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nyc_taxi_lakehouse_dbt:
    +materialized: table
